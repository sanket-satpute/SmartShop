server:
    port: 8082

spring:
  application:
      name: PRODUCT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/smart_shop_product
    username: root
    password: root

  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
  config:
    import: "optional:configserver:"
