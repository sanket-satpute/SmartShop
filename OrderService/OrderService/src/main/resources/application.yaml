server:
    port: 8083

spring:
  application:
      name: ORDER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/smart_shop_order
    username: root
    password: root

  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
  config:
    import: "optional:configserver:"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/